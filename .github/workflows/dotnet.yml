# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --no-build --verbosity normal
    # Note: --no-build flag removed due to .NET 10 RC1 bug with Native AOT compilation
    # Error: "The PrivateSdkAssemblies ItemGroup is required for _ComputeAssembliesToCompileToNative"
    - name: Publish win-x64
      run: dotnet publish -c release -r win-x64
    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v4
      with:
        path: bin/release/net10.0/win-x64/publish/pwsh-prompt.exe

